## 项目信息

- **项目名称**：{{PROJECT_NAME}}
- **项目类型**：（待根据项目实际情况填写）
- **支持平台**：（待根据项目实际情况填写）

## 技术栈

> 完整技术栈、外部依赖、测试配置、AI 工作约束详见 [tech-stack.md](tech-stack.md)。本节不再重复。

---

# 必读核心（启动时加载）

## 开发注意事项

> **本节是 AGENTS.md 约束的唯一全文来源。** AGENTS.md 只放摘要，详细规则以此表为准。

| 规范 | 禁止 |
|------|------|
| 遵循项目现有代码风格和命名约定 | 引入项目未使用的新框架或库 |
| 修改前先理解上下文 | 盲目重构不相关部分 |
| 写入文件使用 MCP 工具直接读写文本方式，大文件分段写入 | 使用内置 write/edit 工具一次性写入整个文件 |
| 终端命令必须加 `rtk` 前缀（如已安装 RTK） | 直接运行未经 rtk 的构建/测试命令 |

## 常用命令

> 所有命令必须加 `rtk` 前缀（如已安装 RTK），详见 `.opencode/skills/rtk/SKILL.md`。

```bash
# （待根据项目实际情况填写构建、测试、运行命令）
```

## 文档同步规则

- 新增或删除源码文件/目录后，更新 `directory-tree.md`
- 技术栈有变更时，必须同步更新 `tech-stack.md`
- 有踩坑经验、有价值信息可以更新到 `MEMORY.md`
- 各文档遵循"单一来源"原则：技术栈唯一来源是 `tech-stack.md`，不重复写版本号和依赖表

---

# 按需参考（任务触发时读取）

## 关键文件位置

### 入口与配置
| 文件 | 路径 |
|------|------|
| （待根据项目实际情况填写） | |

## 技术决策记录

| 决策 | 选择 | 原因 |
|------|------|------|
| （待根据项目实际情况填写） | | |

## 项目经验

> 从各 Plan 实施中提炼的全局经验，后续 Plan/实施必须遵守。

| 规则 | 说明 |
|------|------|
| 最小正确改动 | 只改必要的，不重构不相关部分 |
| 有价值信息先记录再回答 | 不记录 → 下次会话重复踩坑 |
| 构建验证 → 测试验证 → 文档同步 | 不验证 → 引入构建失败 |
